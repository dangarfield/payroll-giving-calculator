{
    "name": "payroll-giving-calculator",
    "version": "0.1.0",
    "dependencies": {},
    "devDependencies": {},
    "scripts": {
        "start": "serve",
        "deploy": "now --public --token $NOW_TOKEN_PAYROLL && now alias --token $NOW_TOKEN_PAYROLL && NOW_TOKEN=${NOW_TOKEN_PAYROLL} now-pipeline-prune && now ls payroll-giving-calculator --token $NOW_TOKEN_PAYROLL"
    }
}